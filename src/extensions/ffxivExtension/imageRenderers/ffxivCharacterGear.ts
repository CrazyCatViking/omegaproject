import { useGranvas } from "../../../utility/granvas-js"

// Implement interface for data
export const renderGear = (data: any) => {
  const { renderImage, addElement } = useGranvas({ width: 1280, height: 720 });
}